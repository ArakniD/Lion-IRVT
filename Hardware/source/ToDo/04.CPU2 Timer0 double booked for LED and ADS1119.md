## Update CPU2 Time 0 over-allocation to prepare for LED Production enablement

Fix: wrap the ADS1119 timer setup in #if (BTS_LED_DRIVER_ENABLED == false), or move the dwell timebase to CPU Timer 2, which is unused on CPU2.

Move the LED timer to un-used CPU Timer 2 on CPU2